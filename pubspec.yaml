name: khuzdar_admin_panel
description: "Khuzdar Services admin panel built with Flutter."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cloud_firestore: ^6.2.0
  cloud_functions: ^6.1.0
  data_table_2: ^2.7.2
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  firebase_messaging: ^16.1.3
  flutter:
    sdk: flutter
  go_router: ^17.2.0
  google_fonts: ^8.0.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
